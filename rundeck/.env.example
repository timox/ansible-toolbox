# =============================================================================
# RUNDECK Configuration
# =============================================================================
# Copier ce fichier vers .env et adapter les valeurs
# =============================================================================

# --- Rundeck Server ---
RUNDECK_VERSION=5.19.0
RUNDECK_PORT=4440
RUNDECK_URL=http://localhost:4440
# Pour accès externe: RUNDECK_URL=https://rundeck.example.com

# --- Admin User ---
RUNDECK_ADMIN_USER=admin
RUNDECK_ADMIN_PASS=changeme

# --- PostgreSQL Database ---
RUNDECK_DB_NAME=rundeck
RUNDECK_DB_USER=rundeck
RUNDECK_DB_PASS=

# --- Ansible Toolbox Path ---
# Chemin vers le dépôt ansible-toolbox (monté en lecture seule)
ANSIBLE_TOOLBOX_PATH=..

# =============================================================================
# NOTES
# =============================================================================
#
# 1. Générer un mot de passe DB sécurisé:
#    openssl rand -base64 24
#
# 2. Premier démarrage:
#    docker compose up -d
#    # Attendre ~2 minutes pour l'initialisation
#
# 3. Accès:
#    http://localhost:4440
#    Login: admin / (RUNDECK_ADMIN_PASS)
#
# 4. Configuration Ansible dans Rundeck:
#    - Aller dans System Menu > System Configuration
#    - Ou créer un projet avec Ansible Node Executor
#
# =============================================================================
