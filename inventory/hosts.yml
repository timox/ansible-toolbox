---
all:
  children:
    remote:
      hosts:
        vps-ovh:
          ansible_host: 92.222.171.85
          ansible_user: ubuntu
          ansible_port: 22

    portal_servers:
      hosts:
        vps-ovh:
          ansible_host: 92.222.171.85
          ansible_user: ubuntu
          ansible_port: 22

    professeur_servers:
      hosts:
        vps-professeur:
          ansible_host: 92.222.171.85
          ansible_user: ubuntu
          ansible_port: 22

    zabbix_agents:
      hosts:
        # Exemples - adapter aux serveurs reels
        # srv-web-01:
        #   ansible_host: 192.168.1.10
        #   ansible_user: ubuntu
        # srv-db-01:
        #   ansible_host: 192.168.1.20
        #   ansible_user: ubuntu

    network:
      children:
        switches:
          hosts:
            # Exemples - adapter aux equipements reels
            # sw-core-01:
            #   ansible_host: 192.168.1.1
            # sw-access-01:
            #   ansible_host: 192.168.1.2

    # ── anonXchange (2-VPS Tor architecture) ──
    anonxchange:
      children:
        anonxchange_bounce:
          hosts:
            bounce-vps:
              ansible_host: "192.168.122.10"
              ansible_user: debian
              ansible_port: 22
        anonxchange_app:
          hosts:
            app-vps:
              ansible_host: "10.0.0.2"
              ansible_user: debian
              ansible_port: 22

    # ── KVM Hypervisor (for test VM provisioning) ──
    kvm_hosts:
      hosts:
        kvm-host:
          ansible_host: "192.168.1.78"
          ansible_user: timo
          ansible_port: 22
