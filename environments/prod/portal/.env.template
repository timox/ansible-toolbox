# Configuration Portail Applications oauth2-proxy
# Copier ce fichier vers .env et ajuster les valeurs

# Domaine principal
DOMAIN=example.com

# Certificats SSL (wildcard)
# Le portail utilise les mêmes certificats que oauth2-proxy
TLS_CERT_FILE=/data/certs/wildcard.example.com.crt
TLS_KEY_FILE=/data/certs/wildcard.example.com.key

# Note: Les variables POMERIUM_TLS_* sont conservées pour compatibilité
# avec l'infrastructure existante. Elles seront renommées dans une
# version future.
