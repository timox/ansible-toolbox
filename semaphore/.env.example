# =============================================================================
# Configuration Semaphore - ansible-toolbox
# =============================================================================
# Copier vers .env et remplir les valeurs
# Usage: cp semaphore/.env.example semaphore/.env

# === SEMAPHORE ===
SEMAPHORE_PORT=3000
SEMAPHORE_ADMIN_USER=admin
SEMAPHORE_ADMIN_NAME=Admin
SEMAPHORE_ADMIN_EMAIL=admin@example.com
SEMAPHORE_ADMIN_PASSWORD=

# Encryption key for stored credentials (generate with: head -c32 /dev/urandom | base64)
SEMAPHORE_ACCESS_KEY_ENCRYPTION=

# === DATABASE ===
SEMAPHORE_DB_USER=semaphore
SEMAPHORE_DB_PASS=
SEMAPHORE_DB_NAME=semaphore

# === GIT ===
GIT_REPO_URL=https://github.com/timox/ansible-toolbox.git
GIT_BRANCH=main

# === PROJETS SEMAPHORE ===
PROJECT_INFRA=Infrastructure
PROJECT_PORTAL=Portail Securise
PROJECT_MONITORING=Monitoring
PROJECT_NETWORK=Network Backup

# === CREDENTIALS (pour setup-semaphore.sh) ===
# Mot de passe Ansible Vault (contenu de .vault-password)
VAULT_PASSWORD=

# SSH : utilisateur pour serveurs distants (remote, zabbix_agents, portal_servers)
SSH_USER=ubuntu
# Cle privee SSH (contenu complet, y compris BEGIN/END)
# Astuce: SSH_PRIVATE_KEY=$(cat ~/.ssh/id_ansible_toolbox)
SSH_PRIVATE_KEY=

# Cisco : credentials equipements reseau (projet Network Backup)
CISCO_USER=admin
CISCO_PASSWORD=
