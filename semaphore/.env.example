# =============================================================================
# Configuration Semaphore API - ansible-toolbox
# =============================================================================
# Copier vers .env et remplir les valeurs
# Usage: cp semaphore/.env.example semaphore/.env

# === SEMAPHORE ===
SEMAPHORE_URL=http://localhost:3000
SEMAPHORE_ADMIN_USER=admin
SEMAPHORE_ADMIN_PASSWORD=

# === PROJET ===
PROJECT_NAME="Ansible Toolbox"
GIT_REPO_URL=https://github.com/timox/ansible-toolbox.git
GIT_BRANCH=main

# === CREDENTIALS ===
# Vault password (contenu de .vault-password)
VAULT_PASSWORD=

# SSH : utilisateur pour serveurs distants (zabbix_agents, remote)
SSH_USER=ubuntu
# Cle privee SSH (contenu complet, y compris BEGIN/END)
# Astuce: SSH_PRIVATE_KEY=$(cat ~/.ssh/id_ansible_toolbox)
SSH_PRIVATE_KEY=

# Cisco : credentials equipements reseau
CISCO_USER=admin
CISCO_PASSWORD=
