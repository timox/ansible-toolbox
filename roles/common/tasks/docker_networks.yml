---
# =============================================================================
# Docker network creation - Equivalent deploy.sh lines 1532-1541
# =============================================================================

- name: Create Docker networks
  community.docker.docker_network:
    name: "{{ item }}"
    driver: bridge
    state: present
  loop: "{{ docker_networks }}"
