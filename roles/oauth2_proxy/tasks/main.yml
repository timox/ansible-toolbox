---
# =============================================================================
# oauth2_proxy Role - Main Tasks
# =============================================================================
# Router file for oauth2-proxy deployment tasks

- name: Include config generation tasks
  ansible.builtin.include_tasks: generate_configs.yml
  tags:
    - oauth2_proxy
    - oauth2_proxy_config

- name: Include nginx config management tasks
  ansible.builtin.include_tasks: manage_nginx_configs.yml
  tags:
    - oauth2_proxy
    - oauth2_proxy_nginx

- name: Include docker compose deployment tasks
  ansible.builtin.include_tasks: deploy.yml
  tags:
    - oauth2_proxy
    - oauth2_proxy_deploy
