---
# =============================================================================
# DOCKER_APP ROLE - Metadata
# =============================================================================

galaxy_info:
  role_name: docker_app
  author: Portail Securise Team
  description: Generic role for deploying Docker Compose applications
  license: MIT
  min_ansible_version: "2.12"

  platforms:
    - name: Debian
      versions:
        - bullseye
        - bookworm
    - name: Ubuntu
      versions:
        - focal
        - jammy

  galaxy_tags:
    - docker
    - dockercompose
    - deployment

dependencies: []
  # This role has no role dependencies
  # It requires community.docker collection (specified in requirements.yml)
