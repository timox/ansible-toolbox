---
# =============================================================================
# GUACAMOLE ROLE - Metadata
# =============================================================================

galaxy_info:
  role_name: guacamole
  author: Portail Securise Team
  description: Deploy Guacamole bastion with OIDC authentication
  license: MIT
  min_ansible_version: "2.12"

  platforms:
    - name: Debian
      versions:
        - bullseye
        - bookworm
    - name: Ubuntu
      versions:
        - focal
        - jammy

  galaxy_tags:
    - guacamole
    - bastion
    - rdp
    - ssh
    - vnc
    - oidc
    - docker

dependencies: []
  # This role has no role dependencies
  # It requires community.docker collection (specified in requirements.yml)
